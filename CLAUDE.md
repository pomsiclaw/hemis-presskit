Este proyecto utiliza SVGs independientes para el logo y la carita en el hero y el header.

Los SVGs están en la carpeta `public/` y deben usarse en vez de texto para mantener la identidad visual.

Consulta AGENTS.md para detalles técnicos y de estructura.
